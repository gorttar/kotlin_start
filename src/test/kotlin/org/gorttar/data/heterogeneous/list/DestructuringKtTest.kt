/**
 * !!!This file is auto generated! All changes to it will be overwritten after running generator!!!
 */

package org.gorttar.data.heterogeneous.list

import assertk.assertAll
import assertk.assertThat
import assertk.assertions.isEqualTo
import org.junit.jupiter.api.Test

class DestructuringKtTest {
    @Test
    fun `xs1 destructuring`() = assertAll {
        val (_a) = xs1
        assertThat(_a).isEqualTo(a)
    }

    @Test
    fun `xs2 destructuring`() = assertAll {
        val (_a, _b) = xs2
        assertThat(_a).isEqualTo(a)
        assertThat(_b).isEqualTo(b)
    }

    @Test
    fun `xs3 destructuring`() = assertAll {
        val (_a, _b, _c) = xs3
        assertThat(_a).isEqualTo(a)
        assertThat(_b).isEqualTo(b)
        assertThat(_c).isEqualTo(c)
    }

    @Test
    fun `xs4 destructuring`() = assertAll {
        val (_a, _b, _c, _d) = xs4
        assertThat(_a).isEqualTo(a)
        assertThat(_b).isEqualTo(b)
        assertThat(_c).isEqualTo(c)
        assertThat(_d).isEqualTo(d)
    }

    @Test
    fun `xs5 destructuring`() = assertAll {
        val (_a, _b, _c, _d, _e) = xs5
        assertThat(_a).isEqualTo(a)
        assertThat(_b).isEqualTo(b)
        assertThat(_c).isEqualTo(c)
        assertThat(_d).isEqualTo(d)
        assertThat(_e).isEqualTo(e)
    }

    @Test
    fun `xs6 destructuring`() = assertAll {
        val (_a, _b, _c, _d, _e, _f) = xs6
        assertThat(_a).isEqualTo(a)
        assertThat(_b).isEqualTo(b)
        assertThat(_c).isEqualTo(c)
        assertThat(_d).isEqualTo(d)
        assertThat(_e).isEqualTo(e)
        assertThat(_f).isEqualTo(f)
    }

    @Test
    fun `xs7 destructuring`() = assertAll {
        val (_a, _b, _c, _d, _e, _f, _g) = xs7
        assertThat(_a).isEqualTo(a)
        assertThat(_b).isEqualTo(b)
        assertThat(_c).isEqualTo(c)
        assertThat(_d).isEqualTo(d)
        assertThat(_e).isEqualTo(e)
        assertThat(_f).isEqualTo(f)
        assertThat(_g).isEqualTo(g)
    }

    @Test
    fun `xs8 destructuring`() = assertAll {
        val (_a, _b, _c, _d, _e, _f, _g, _h) = xs8
        assertThat(_a).isEqualTo(a)
        assertThat(_b).isEqualTo(b)
        assertThat(_c).isEqualTo(c)
        assertThat(_d).isEqualTo(d)
        assertThat(_e).isEqualTo(e)
        assertThat(_f).isEqualTo(f)
        assertThat(_g).isEqualTo(g)
        assertThat(_h).isEqualTo(h)
    }

    @Test
    fun `xs9 destructuring`() = assertAll {
        val (_a, _b, _c, _d, _e, _f, _g, _h, _i) = xs9
        assertThat(_a).isEqualTo(a)
        assertThat(_b).isEqualTo(b)
        assertThat(_c).isEqualTo(c)
        assertThat(_d).isEqualTo(d)
        assertThat(_e).isEqualTo(e)
        assertThat(_f).isEqualTo(f)
        assertThat(_g).isEqualTo(g)
        assertThat(_h).isEqualTo(h)
        assertThat(_i).isEqualTo(i)
    }

    @Test
    fun `xs10 destructuring`() = assertAll {
        val (_a, _b, _c, _d, _e, _f, _g, _h, _i, _j) = xs10
        assertThat(_a).isEqualTo(a)
        assertThat(_b).isEqualTo(b)
        assertThat(_c).isEqualTo(c)
        assertThat(_d).isEqualTo(d)
        assertThat(_e).isEqualTo(e)
        assertThat(_f).isEqualTo(f)
        assertThat(_g).isEqualTo(g)
        assertThat(_h).isEqualTo(h)
        assertThat(_i).isEqualTo(i)
        assertThat(_j).isEqualTo(j)
    }

    @Test
    fun `xs11 destructuring`() = assertAll {
        val (_a, _b, _c, _d, _e, _f, _g, _h, _i, _j, _k) = xs11
        assertThat(_a).isEqualTo(a)
        assertThat(_b).isEqualTo(b)
        assertThat(_c).isEqualTo(c)
        assertThat(_d).isEqualTo(d)
        assertThat(_e).isEqualTo(e)
        assertThat(_f).isEqualTo(f)
        assertThat(_g).isEqualTo(g)
        assertThat(_h).isEqualTo(h)
        assertThat(_i).isEqualTo(i)
        assertThat(_j).isEqualTo(j)
        assertThat(_k).isEqualTo(k)
    }

    @Test
    fun `xs12 destructuring`() = assertAll {
        val (_a, _b, _c, _d, _e, _f, _g, _h, _i, _j, _k, _l) = xs12
        assertThat(_a).isEqualTo(a)
        assertThat(_b).isEqualTo(b)
        assertThat(_c).isEqualTo(c)
        assertThat(_d).isEqualTo(d)
        assertThat(_e).isEqualTo(e)
        assertThat(_f).isEqualTo(f)
        assertThat(_g).isEqualTo(g)
        assertThat(_h).isEqualTo(h)
        assertThat(_i).isEqualTo(i)
        assertThat(_j).isEqualTo(j)
        assertThat(_k).isEqualTo(k)
        assertThat(_l).isEqualTo(l)
    }

    @Test
    fun `xs13 destructuring`() = assertAll {
        val (_a, _b, _c, _d, _e, _f, _g, _h, _i, _j, _k, _l, _m) = xs13
        assertThat(_a).isEqualTo(a)
        assertThat(_b).isEqualTo(b)
        assertThat(_c).isEqualTo(c)
        assertThat(_d).isEqualTo(d)
        assertThat(_e).isEqualTo(e)
        assertThat(_f).isEqualTo(f)
        assertThat(_g).isEqualTo(g)
        assertThat(_h).isEqualTo(h)
        assertThat(_i).isEqualTo(i)
        assertThat(_j).isEqualTo(j)
        assertThat(_k).isEqualTo(k)
        assertThat(_l).isEqualTo(l)
        assertThat(_m).isEqualTo(m)
    }

    @Test
    fun `xs14 destructuring`() = assertAll {
        val (_a, _b, _c, _d, _e, _f, _g, _h, _i, _j, _k, _l, _m, _n) = xs14
        assertThat(_a).isEqualTo(a)
        assertThat(_b).isEqualTo(b)
        assertThat(_c).isEqualTo(c)
        assertThat(_d).isEqualTo(d)
        assertThat(_e).isEqualTo(e)
        assertThat(_f).isEqualTo(f)
        assertThat(_g).isEqualTo(g)
        assertThat(_h).isEqualTo(h)
        assertThat(_i).isEqualTo(i)
        assertThat(_j).isEqualTo(j)
        assertThat(_k).isEqualTo(k)
        assertThat(_l).isEqualTo(l)
        assertThat(_m).isEqualTo(m)
        assertThat(_n).isEqualTo(n)
    }

    @Test
    fun `xs15 destructuring`() = assertAll {
        val (_a, _b, _c, _d, _e, _f, _g, _h, _i, _j, _k, _l, _m, _n, _o) = xs15
        assertThat(_a).isEqualTo(a)
        assertThat(_b).isEqualTo(b)
        assertThat(_c).isEqualTo(c)
        assertThat(_d).isEqualTo(d)
        assertThat(_e).isEqualTo(e)
        assertThat(_f).isEqualTo(f)
        assertThat(_g).isEqualTo(g)
        assertThat(_h).isEqualTo(h)
        assertThat(_i).isEqualTo(i)
        assertThat(_j).isEqualTo(j)
        assertThat(_k).isEqualTo(k)
        assertThat(_l).isEqualTo(l)
        assertThat(_m).isEqualTo(m)
        assertThat(_n).isEqualTo(n)
        assertThat(_o).isEqualTo(o)
    }

    @Test
    fun `xs16 destructuring`() = assertAll {
        val (_a, _b, _c, _d, _e, _f, _g, _h, _i, _j, _k, _l, _m, _n, _o, _p) = xs16
        assertThat(_a).isEqualTo(a)
        assertThat(_b).isEqualTo(b)
        assertThat(_c).isEqualTo(c)
        assertThat(_d).isEqualTo(d)
        assertThat(_e).isEqualTo(e)
        assertThat(_f).isEqualTo(f)
        assertThat(_g).isEqualTo(g)
        assertThat(_h).isEqualTo(h)
        assertThat(_i).isEqualTo(i)
        assertThat(_j).isEqualTo(j)
        assertThat(_k).isEqualTo(k)
        assertThat(_l).isEqualTo(l)
        assertThat(_m).isEqualTo(m)
        assertThat(_n).isEqualTo(n)
        assertThat(_o).isEqualTo(o)
        assertThat(_p).isEqualTo(p)
    }

    @Test
    fun `xs17 destructuring`() = assertAll {
        val (_a, _b, _c, _d, _e, _f, _g, _h, _i, _j, _k, _l, _m, _n, _o, _p, _q) = xs17
        assertThat(_a).isEqualTo(a)
        assertThat(_b).isEqualTo(b)
        assertThat(_c).isEqualTo(c)
        assertThat(_d).isEqualTo(d)
        assertThat(_e).isEqualTo(e)
        assertThat(_f).isEqualTo(f)
        assertThat(_g).isEqualTo(g)
        assertThat(_h).isEqualTo(h)
        assertThat(_i).isEqualTo(i)
        assertThat(_j).isEqualTo(j)
        assertThat(_k).isEqualTo(k)
        assertThat(_l).isEqualTo(l)
        assertThat(_m).isEqualTo(m)
        assertThat(_n).isEqualTo(n)
        assertThat(_o).isEqualTo(o)
        assertThat(_p).isEqualTo(p)
        assertThat(_q).isEqualTo(q)
    }

    @Test
    fun `xs18 destructuring`() = assertAll {
        val (_a, _b, _c, _d, _e, _f, _g, _h, _i, _j, _k, _l, _m, _n, _o, _p, _q, _r) = xs18
        assertThat(_a).isEqualTo(a)
        assertThat(_b).isEqualTo(b)
        assertThat(_c).isEqualTo(c)
        assertThat(_d).isEqualTo(d)
        assertThat(_e).isEqualTo(e)
        assertThat(_f).isEqualTo(f)
        assertThat(_g).isEqualTo(g)
        assertThat(_h).isEqualTo(h)
        assertThat(_i).isEqualTo(i)
        assertThat(_j).isEqualTo(j)
        assertThat(_k).isEqualTo(k)
        assertThat(_l).isEqualTo(l)
        assertThat(_m).isEqualTo(m)
        assertThat(_n).isEqualTo(n)
        assertThat(_o).isEqualTo(o)
        assertThat(_p).isEqualTo(p)
        assertThat(_q).isEqualTo(q)
        assertThat(_r).isEqualTo(r)
    }

    @Test
    fun `xs19 destructuring`() = assertAll {
        val (_a, _b, _c, _d, _e, _f, _g, _h, _i, _j, _k, _l, _m, _n, _o, _p, _q, _r, _s) = xs19
        assertThat(_a).isEqualTo(a)
        assertThat(_b).isEqualTo(b)
        assertThat(_c).isEqualTo(c)
        assertThat(_d).isEqualTo(d)
        assertThat(_e).isEqualTo(e)
        assertThat(_f).isEqualTo(f)
        assertThat(_g).isEqualTo(g)
        assertThat(_h).isEqualTo(h)
        assertThat(_i).isEqualTo(i)
        assertThat(_j).isEqualTo(j)
        assertThat(_k).isEqualTo(k)
        assertThat(_l).isEqualTo(l)
        assertThat(_m).isEqualTo(m)
        assertThat(_n).isEqualTo(n)
        assertThat(_o).isEqualTo(o)
        assertThat(_p).isEqualTo(p)
        assertThat(_q).isEqualTo(q)
        assertThat(_r).isEqualTo(r)
        assertThat(_s).isEqualTo(s)
    }

    @Test
    fun `xs20 destructuring`() = assertAll {
        val (_a, _b, _c, _d, _e, _f, _g, _h, _i, _j, _k, _l, _m, _n, _o, _p, _q, _r, _s, _t) = xs20
        assertThat(_a).isEqualTo(a)
        assertThat(_b).isEqualTo(b)
        assertThat(_c).isEqualTo(c)
        assertThat(_d).isEqualTo(d)
        assertThat(_e).isEqualTo(e)
        assertThat(_f).isEqualTo(f)
        assertThat(_g).isEqualTo(g)
        assertThat(_h).isEqualTo(h)
        assertThat(_i).isEqualTo(i)
        assertThat(_j).isEqualTo(j)
        assertThat(_k).isEqualTo(k)
        assertThat(_l).isEqualTo(l)
        assertThat(_m).isEqualTo(m)
        assertThat(_n).isEqualTo(n)
        assertThat(_o).isEqualTo(o)
        assertThat(_p).isEqualTo(p)
        assertThat(_q).isEqualTo(q)
        assertThat(_r).isEqualTo(r)
        assertThat(_s).isEqualTo(s)
        assertThat(_t).isEqualTo(t)
    }

    @Test
    fun `xs21 destructuring`() = assertAll {
        val (_a, _b, _c, _d, _e, _f, _g, _h, _i, _j, _k, _l, _m, _n, _o, _p, _q, _r, _s, _t, _u) = xs21
        assertThat(_a).isEqualTo(a)
        assertThat(_b).isEqualTo(b)
        assertThat(_c).isEqualTo(c)
        assertThat(_d).isEqualTo(d)
        assertThat(_e).isEqualTo(e)
        assertThat(_f).isEqualTo(f)
        assertThat(_g).isEqualTo(g)
        assertThat(_h).isEqualTo(h)
        assertThat(_i).isEqualTo(i)
        assertThat(_j).isEqualTo(j)
        assertThat(_k).isEqualTo(k)
        assertThat(_l).isEqualTo(l)
        assertThat(_m).isEqualTo(m)
        assertThat(_n).isEqualTo(n)
        assertThat(_o).isEqualTo(o)
        assertThat(_p).isEqualTo(p)
        assertThat(_q).isEqualTo(q)
        assertThat(_r).isEqualTo(r)
        assertThat(_s).isEqualTo(s)
        assertThat(_t).isEqualTo(t)
        assertThat(_u).isEqualTo(u)
    }

    @Test
    fun `xs22 destructuring`() = assertAll {
        val (_a, _b, _c, _d, _e, _f, _g, _h, _i, _j, _k, _l, _m, _n, _o, _p, _q, _r, _s, _t, _u, _v) = xs22
        assertThat(_a).isEqualTo(a)
        assertThat(_b).isEqualTo(b)
        assertThat(_c).isEqualTo(c)
        assertThat(_d).isEqualTo(d)
        assertThat(_e).isEqualTo(e)
        assertThat(_f).isEqualTo(f)
        assertThat(_g).isEqualTo(g)
        assertThat(_h).isEqualTo(h)
        assertThat(_i).isEqualTo(i)
        assertThat(_j).isEqualTo(j)
        assertThat(_k).isEqualTo(k)
        assertThat(_l).isEqualTo(l)
        assertThat(_m).isEqualTo(m)
        assertThat(_n).isEqualTo(n)
        assertThat(_o).isEqualTo(o)
        assertThat(_p).isEqualTo(p)
        assertThat(_q).isEqualTo(q)
        assertThat(_r).isEqualTo(r)
        assertThat(_s).isEqualTo(s)
        assertThat(_t).isEqualTo(t)
        assertThat(_u).isEqualTo(u)
        assertThat(_v).isEqualTo(v)
    }

    @Test
    fun `xs23 destructuring`() = assertAll {
        val (_a, _b, _c, _d, _e, _f, _g, _h, _i, _j, _k, _l, _m, _n, _o, _p, _q, _r, _s, _t, _u, _v, _w) = xs23
        assertThat(_a).isEqualTo(a)
        assertThat(_b).isEqualTo(b)
        assertThat(_c).isEqualTo(c)
        assertThat(_d).isEqualTo(d)
        assertThat(_e).isEqualTo(e)
        assertThat(_f).isEqualTo(f)
        assertThat(_g).isEqualTo(g)
        assertThat(_h).isEqualTo(h)
        assertThat(_i).isEqualTo(i)
        assertThat(_j).isEqualTo(j)
        assertThat(_k).isEqualTo(k)
        assertThat(_l).isEqualTo(l)
        assertThat(_m).isEqualTo(m)
        assertThat(_n).isEqualTo(n)
        assertThat(_o).isEqualTo(o)
        assertThat(_p).isEqualTo(p)
        assertThat(_q).isEqualTo(q)
        assertThat(_r).isEqualTo(r)
        assertThat(_s).isEqualTo(s)
        assertThat(_t).isEqualTo(t)
        assertThat(_u).isEqualTo(u)
        assertThat(_v).isEqualTo(v)
        assertThat(_w).isEqualTo(w)
    }

    @Test
    fun `xs24 destructuring`() = assertAll {
        val (_a, _b, _c, _d, _e, _f, _g, _h, _i, _j, _k, _l, _m, _n, _o, _p, _q, _r, _s, _t, _u, _v, _w, _x) = xs24
        assertThat(_a).isEqualTo(a)
        assertThat(_b).isEqualTo(b)
        assertThat(_c).isEqualTo(c)
        assertThat(_d).isEqualTo(d)
        assertThat(_e).isEqualTo(e)
        assertThat(_f).isEqualTo(f)
        assertThat(_g).isEqualTo(g)
        assertThat(_h).isEqualTo(h)
        assertThat(_i).isEqualTo(i)
        assertThat(_j).isEqualTo(j)
        assertThat(_k).isEqualTo(k)
        assertThat(_l).isEqualTo(l)
        assertThat(_m).isEqualTo(m)
        assertThat(_n).isEqualTo(n)
        assertThat(_o).isEqualTo(o)
        assertThat(_p).isEqualTo(p)
        assertThat(_q).isEqualTo(q)
        assertThat(_r).isEqualTo(r)
        assertThat(_s).isEqualTo(s)
        assertThat(_t).isEqualTo(t)
        assertThat(_u).isEqualTo(u)
        assertThat(_v).isEqualTo(v)
        assertThat(_w).isEqualTo(w)
        assertThat(_x).isEqualTo(x)
    }

    @Test
    fun `xs25 destructuring`() = assertAll {
        val (_a, _b, _c, _d, _e, _f, _g, _h, _i, _j, _k, _l, _m, _n, _o, _p, _q, _r, _s, _t, _u, _v, _w, _x, _y) = xs25
        assertThat(_a).isEqualTo(a)
        assertThat(_b).isEqualTo(b)
        assertThat(_c).isEqualTo(c)
        assertThat(_d).isEqualTo(d)
        assertThat(_e).isEqualTo(e)
        assertThat(_f).isEqualTo(f)
        assertThat(_g).isEqualTo(g)
        assertThat(_h).isEqualTo(h)
        assertThat(_i).isEqualTo(i)
        assertThat(_j).isEqualTo(j)
        assertThat(_k).isEqualTo(k)
        assertThat(_l).isEqualTo(l)
        assertThat(_m).isEqualTo(m)
        assertThat(_n).isEqualTo(n)
        assertThat(_o).isEqualTo(o)
        assertThat(_p).isEqualTo(p)
        assertThat(_q).isEqualTo(q)
        assertThat(_r).isEqualTo(r)
        assertThat(_s).isEqualTo(s)
        assertThat(_t).isEqualTo(t)
        assertThat(_u).isEqualTo(u)
        assertThat(_v).isEqualTo(v)
        assertThat(_w).isEqualTo(w)
        assertThat(_x).isEqualTo(x)
        assertThat(_y).isEqualTo(y)
    }

    @Test
    fun `xs26 destructuring`() = assertAll {
        val (_a, _b, _c, _d, _e, _f, _g, _h, _i, _j, _k, _l, _m, _n, _o, _p, _q, _r, _s, _t, _u, _v, _w, _x, _y, _z) = xs26
        assertThat(_a).isEqualTo(a)
        assertThat(_b).isEqualTo(b)
        assertThat(_c).isEqualTo(c)
        assertThat(_d).isEqualTo(d)
        assertThat(_e).isEqualTo(e)
        assertThat(_f).isEqualTo(f)
        assertThat(_g).isEqualTo(g)
        assertThat(_h).isEqualTo(h)
        assertThat(_i).isEqualTo(i)
        assertThat(_j).isEqualTo(j)
        assertThat(_k).isEqualTo(k)
        assertThat(_l).isEqualTo(l)
        assertThat(_m).isEqualTo(m)
        assertThat(_n).isEqualTo(n)
        assertThat(_o).isEqualTo(o)
        assertThat(_p).isEqualTo(p)
        assertThat(_q).isEqualTo(q)
        assertThat(_r).isEqualTo(r)
        assertThat(_s).isEqualTo(s)
        assertThat(_t).isEqualTo(t)
        assertThat(_u).isEqualTo(u)
        assertThat(_v).isEqualTo(v)
        assertThat(_w).isEqualTo(w)
        assertThat(_x).isEqualTo(x)
        assertThat(_y).isEqualTo(y)
        assertThat(_z).isEqualTo(z)
    }
}